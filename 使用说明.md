# WeRSS 使用说明（本机安装版）

项目来源：https://github.com/rachelos/we-mp-rss
功能：微信公众号文章聚合，自动抓取订阅的公众号文章并生成 RSS 订阅源。

> 📌 项目已整合：根目录 `README.md` 为总览与交接文档；
> `./start-all.sh` / `./stop-all.sh` 现可一键启动/停止全部三个服务（含本服务）。
> 本文件只讲 we-mp-rss 的单独运行与配置细节。

## 服务状态

- 服务已启动：http://127.0.0.1:8001
- 登录账号：`admin`
- 登录密码：`admin@123`（如需修改，见下方"配置"）

## 日常使用

1. 打开 http://127.0.0.1:8001 ，用 admin 账号登录。
2. 在界面中点击"扫码授权"，用微信扫码授权（需要能访问公众号后台的微信）。
3. 授权成功后，添加你想订阅的公众号。
4. 系统定时任务会自动抓取新文章并生成 RSS 链接，在订阅列表里复制 RSS 地址，
   添加到任何 RSS 阅读器（如 Follow、Inoreader、NetNewsWire 等）即可。

## 启动 / 停止

```bash
cd /Users/baiyanglin/Desktop/RSS订阅/we-mp-rss

# 启动（默认账号 admin / admin@123）
./start-local.sh

# 停止（找到进程后结束）
pkill -f "main.py -job"
```

服务数据（SQLite 数据库、缓存、抓取的文章）保存在 `we-mp-rss/data/` 目录。

## 配置

- 主配置：`we-mp-rss/config.yaml`（由 `config.example.yaml` 复制而来）
- 常用环境变量（可在启动前 export）：
  - `USERNAME` / `PASSWORD`：登录账号密码
  - `PORT`：服务端口（默认 8001）
  - `DB`：数据库连接串（默认 SQLite `sqlite:///./data/db.db`）
  - `RSS_BASE_URL`：RSS 链接对外域名（局域网/公网部署时填写）
- 定时抓取、通知渠道（钉钉/企微/飞书 Webhook）等都在 config.yaml 中配置。

## 本机安装说明（macOS / Python 3.14）

原版 `requirements.txt` 锁定的部分包（pydantic、pillow、greenlet 等）没有
Python 3.14 的预编译包，本机安装时使用了 `requirements-314.txt`（放开这些包
的版本锁定，并去掉未用到的 psycopg2 / PostgreSQL 依赖）。依赖已装进
`we-mp-rss/.venv/`，无需重装。

重装依赖：
```bash
cd /Users/baiyanglin/Desktop/RSS订阅/we-mp-rss
.venv/bin/pip install -r requirements-314.txt
```

Playwright 浏览器内核（chromium + webkit）已装到 `~/Library/Caches/ms-playwright`，
用于抓取公众号文章。
