# 信息聚合站 使用说明

本目录基于 [RSSHub](https://github.com/DIYgod/RSSHub)（pnpm 源码直跑）+ 自建聚合网页，
与 we-mp-rss（微信公众号 RSS，端口 8001）共同构成信息聚合体系。

## 服务一览

| 服务 | 端口 | 说明 | 启动方式 |
| ---- | ---- | ---- | -------- |
| we-mp-rss | 8001 | 微信公众号聚合 | `cd we-mp-rss && ./start-local.sh` |
| RSSHub | 1200 | 生成各类信息源 RSS | `cd rsshub && pnpm dev` |
| 聚合站点 | 3000 | 自建聚合网页 | `cd aggr-site && node server.js` |

> 提示：根目录 `README.md` 为项目总览与交接文档。
> `./start-all.sh` / `./stop-all.sh` 现已覆盖**全部三个服务**（含 we-mp-rss），
> 已启动过的服务会自动跳过；本文件只讲 RSSHub 与聚合站点的细节。

## 日常使用

1. 打开 http://127.0.0.1:3000 —— 聚合网页，按时间流展示全部源的最新内容。
2. 左侧勾选/取消勾选信息源，顶部切换时间范围（近 6 小时 ~ 全部）。
3. 点"⟳ 刷新"或等 5 分钟自动刷新；"源健康检查"可看每个源的抓取状态。

## 添加信息源

编辑 `aggr-site/sources.json`，按现有格式追加一项，然后刷新网页即可：

```json
{
  "id": "唯一标识",
  "name": "显示名称",
  "type": "rsshub 或 rss",
  "url": "RSS 地址（RSSHub 路由或任意 RSS 源）",
  "category": "分类（决定侧栏颜色）",
  "enabled": true
}
```

找 RSSHub 路由：http://127.0.0.1:1200 首页可搜索路由；文档见 https://docs.rsshub.app 。
注意部分路由需要额外配置（如 GitHub Trending 需 `GITHUB_ACCESS_TOKEN`，
微博/Twitter 等需要 cookie），配置写在 `rsshub/.env`（从 `.env.example` 复制）。
微信公众号的单号源：http://127.0.0.1:8001/rss/{feed_id}/api ，feed_id 在
we-mp-rss 后台订阅列表里查看。

没有现成路由的站点，可以在 RSSHub 里写自定义路由（`rsshub/lib/routes/<站点>/<频道>.ts`，
dev 模式热重载，改完立即生效）。本项目的自定义路由：

- 浙江宣传：`rsshub/lib/routes/zjol/zjxc.ts`（源 https://zjnews.zjol.com.cn/zjxc/）
- 澎湃·马上评：`rsshub/lib/routes/thepaper/studio.ts`（源 https://www.thepaper.cn/studio_20，
  走澎湃官方 API `/contentapi/studio/detailsPageListWww`，可传其他工作室 id）

## 技术说明

- 聚合站点：Node.js 原生 http + fast-xml-parser，零框架；后端统一抓取各源、
  解析（RSS 2.0 / Atom 均支持）、按时间排序去重，前端为原生 HTML/JS/CSS。
- 源抓取有 5 分钟缓存（`CACHE_TTL` 环境变量可调），单源超时 15 秒。
- 接口：`/api/items?hours=24&source=id1,id2&limit=200`、`/api/sources`、`/api/health`。
- RSSHub 目前以 dev 模式（tsx watch）运行，改路由代码会热重载；
  正式部署可用 `pnpm build && pnpm start`。
