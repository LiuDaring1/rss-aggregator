# 口语素材周刊 · Agent 新会话接班与每周出刊标准化指南 (AGENTS.md)

> **这是什么**：本文件是面向任何在当前工作区启动的新 Antigravity Agent 会话的全局接班规则与标准操作规程（SOP）。无论用户在哪个新窗口与你对话，请严格遵循本规范。

---

## 🧭 一、项目定位与当前状态

- **项目目标**：面向高中播音主持艺考生的口语表达训练周刊（合订本 47 物理页，包含：9篇动作叙事复述 + 9幅3:1多格漫画 + 6篇时评思辨双向立论 + 6篇原文表达拆解）。
- **当前版本**：v3.0.0（工作台可靠运行、动态业务窗口与候选池隔离、免责声明全量落地）。
- **基线出刊**：
  - `issue-2026-w38`：首发标杆件（不可篡改，SHA256: `0d21e7d9ab8dfaa22859fae2ab9b7893a6625abff7f7234ea9f4ae521d129010`）
  - `issue-2026-w39`：首次试发件（已出刊 47 页，不可篡改，SHA256: `1e50e174091f0742662f99abef28c9b9d972013f8a758e3f72af6d601e1dc68d`）
  - 当前准备期号：`issue-2026-w40` 及后续常规周次（严格依真实时间推进）。

---

## ⏰ 二、业务时间周期与人机分工原则

1. **真实自然周时间纪律**：
   - **资料截止点**：每周四 20:00:00 (Asia/Shanghai)
   - **出刊发放点**：每周五 10:00:00 (Asia/Shanghai)
   - **回望时间窗**：严格回望截止点前连续 7 天 `[上周四 20:00:00, 本周四 20:00:00)` 的真实报道。
   - **绝对底线**：严禁在周四 20:00 资料窗口未截止前提前锁定或出刊！严禁换期号复用同一批旧材料！

2. **自动运行与 Agent 触发边界**：
   - **周内日常**：macOS launchd 定时任务每天自跑（09:30、18:30），抓取 14 路主流媒体与暖文，本地规则自动初筛入库，工作台服务（`http://127.0.0.1:3002/workbench.html`）后台守护。
   - **出刊制作**：周四 20:00 截稿后，用户在 Antigravity 唤醒 Agent 下达制作指令，Agent 端到端执行写稿、画图、排版与验真。

---

## 🛠️ 三、当用户在新窗口说“开始制作本周周刊 / 截稿了”时的标准执行流程 (SOP)

当你收到出刊指令时，请按顺序执行以下 5 步：

### Step 1: 确定当前目标期号与时间窗口
运行业务窗口计算：
```bash
python3 scripts/business_window.py
```
获取当前周期的 `target_issue_id`（如 `issue-2026-w40`）与起止时间 `[start, cutoff)`。

### Step 2: 调阅本周候选池与教师审阅意见
从本地 API 或数据库读取候选材料：
- 查询 `http://127.0.0.1:3002/api/workbench/articles?pool=current&pageSize=50`
- 读取教师在工作台上持久化勾选的保留/排除反馈：`data/teacher_feedback.json`
- 选材规则：
  - 教师保留（`teacher_approved`）的具有最高优先级，必须入选；
  - 教师排除（`teacher_excluded`）或已用（`isUsed`）的严禁入选；
  - 敏感议题（艾滋病、重大恶性案等）未经教师特批严禁入选；
  - 纯元数据（`pending_facts`）必须补齐清洗正文；
  - 优选 S/A 级重点推荐材料（具备鲜活人物行动冲突宜复述，具备生活思辨争议宜时评）。

### Step 3: 初始化本期并撰写 YAML 数据单元
创建目标刊期目录（如 `issues/issue-2026-w40/`）：
```bash
python3 scripts/weekly_runner.py prep --issue issue-2026-w40
```
加工生成 21 篇内容：
- **9 篇口语复述**（`content/retellings/`）：提炼 9 句核心动作链，语言青年自然，避免僵硬公文腔；
- **9 幅动作叙事漫画**（`illustrations/`）：调用图像生成工具生成 3:1 横排多格动作叙事插画；
- **6 篇时评思辨**（`content/commentaries/`）：严格按照 3 页版面规范（核心争议 + 3~6 个推演角度 + 双主体口语说理范本）；
- **6 篇原文拆解**（`content/excerpts/`）：严格引用真实信源原段，引文段落必须与存档原文 100% 连续精确匹配。

### Step 4: 运行全门禁原子构建与核验
执行统一构建脚本：
```bash
python3 scripts/weekly_runner.py build --issue issue-2026-w40
```
必须通过以下全部门禁（任何一项失败立即阻断出刊）：
1. 采编台账（manifest）与上游信源快照 100% 吻合；
2. 拆解引文与信源原段连续文本 100% 匹配；
3. 事实核验状态全部为 passed（核心人物、时间、事实一致）；
4. 物理页数 47 页守恒，杜绝空白溢出或跨页截断；
5. 生成完整期次 PDF 及三分册 PDF。

### Step 5: 更新状态并交付教师终审
运行发布指令：
```bash
python3 scripts/weekly_runner.py publish --issue issue-2026-w40
```
运行单元测试确保无回归：
```bash
PYTHONPATH=publishing:scripts python3 -m unittest discover -s publishing/tests
```
输出本期总结交付给用户审阅。
