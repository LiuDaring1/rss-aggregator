# 自定义改动清单（相对上游 / 自研部分）

> 用途：交接给下一个 agent 时，明确「哪些文件是本阶段写的」、
> 哪些是上游原样。核对方式：基于 `git status`（未跟踪新增）+ 目录比对。

## 1. rsshub/（git 仓库，以下均为**未跟踪新增**，未修改任何已跟踪文件）

| 文件 | 内容 | 状态 |
| ---- | ---- | ---- |
| `lib/routes/zjol/zjxc.ts` | 浙江宣传（zjnews.zjol.com.cn/zjxc），列表页 + 详情页 | 新增 |
| `lib/routes/thepaper/studio.ts` | 澎湃·工作室（走官方 API `/contentapi/studio/detailsPageListWww`，`studio_20` = 马上评） | 新增 |
| `lib/routes/bjnews/kuaiping.ts` | 新京报快评·风向标（2026-09-03 新增）：源站上阿里云 WAF、405 拦截头部不全请求且封了旧路径 `/point/1.html`，本路由改抓新栏目页 `/point` + 完整浏览器头，单篇详情被拦时降级（只出标题+链接，不炸整条路由） | 新增 |
| `lib/routes/bjnews/utils.ts` | fetchArticle 增加 Referer/Accept-Language 等浏览器头（2026-09-03 本地补丁，WAF 要求）；详情页 405 时让上层路由降级 | 改动（上游文件打补丁） |
| `lib/routes/gmw/`（`namespace.ts` + `guancha.ts`） | 光明网评论频道栏目（guancha.gmw.cn/node_{id}.htm） | 整目录新增（上游无） |
| `lib/routes/rednet/`（`namespace.ts` + `channel.ts`） | 红网红辣椒评论栏目（hlj.rednet.cn/channel/{id}.html），兼容新旧两种列表模板 | 整目录新增（上游无） |
| `lib/routes/youth/`（`namespace.ts` + `pinglun.ts`） | 中青评论频道（pinglun.youth.cn/{cat}/，GBK 解码），默认 `wztt` 热点 | 整目录新增（上游无） |

注意（容易误判为自研、实际是**上游已有**）：
- `lib/routes/zjol/namespace.ts`、`lib/routes/zjol/paper.ts` —— 上游已有
- `lib/routes/thepaper/` 其余全部文件（namespace.ts、channel.ts、factpaper.tsx、
  featured.ts、gov.ts、list.ts、sidebar.ts、user.ts、utils.tsx、839studio/）—— 上游已有

## 2. we-mp-rss/（git 仓库，均为**未跟踪新增**）

| 文件 | 内容 |
| ---- | ---- |
| `requirements-314.txt` | Python 3.14 适配依赖清单：放开 pydantic/pillow/greenlet 等版本锁定，去掉 psycopg2；实际安装用这个 |
| `requirements-nopg.txt` | 去掉 PostgreSQL 依赖的清单（前期尝试产物，当前实际用 314 版） |
| `start-local.sh` | 本机启动脚本：`cd` 自身目录 → 默认 `USERNAME=admin` / `PASSWORD=admin@123`（环境变量可覆盖）→ `.venv/bin/python main.py -job True -init True` |

本地配置（**未入库、勿提交/勿打包**）：
- `config.yaml`：从 `config.example.yaml` 复制，含环境变量插值（DB、PORT、通知 webhook 等）
- `data/`：SQLite `db.db`、微信授权 `wx.lic`、文件缓存、内置 Redis 数据

## 3. aggr-site/（非 git 仓库，整体 = 本阶段 100% 自研）

| 文件 | 说明 |
| ---- | ---- |
| `server.js` | 后端（Node 原生 http + fast-xml-parser，零框架）：`/api/items`、`/api/sources`、`/api/health`；RSS 2.0 / Atom 统一解析；5 分钟缓存（失败仅 30 秒）；60 秒超时；目录穿越防护 |
| `sources.json` | 信息源配置，当前 11 个源（全部「评论」类） |
| `public/index.html` | 页面结构（顶栏时间范围+刷新 / 左侧源多选 / 右侧时间流卡片） |
| `public/app.js` | 前端逻辑：多选过滤、点源名「单独查看」、6h~全部时间范围、5 分钟自动刷新 |
| `public/style.css` | 样式（含分类色点、卡片、响应式） |
| `package.json` / `pnpm-lock.yaml` | 唯一依赖 `fast-xml-parser` |

## 4. 根目录（本阶段新增/编写的交接收尾文件）

| 文件 | 说明 |
| ---- | ---- |
| `README.md` | 项目总览·交接文档（本次新增） |
| `自定义改动清单.md` | 本文档 |
| `start-all.sh` | 一键启动三个服务（本次升级为含 we-mp-rss） |
| `stop-all.sh` | 一键停止三个服务（本次升级为含 we-mp-rss） |
| `package.sh` | 源码包生成脚本（本次新增） |
| `使用说明.md` / `聚合站使用说明.md` | 之前阶段写的运行说明（保留，内容仍准确） |

## 5. 校验命令（可复跑确认）

```bash
cd rsshub && git status --porcelain          # 应只有上述 5 处未跟踪新增
cd we-mp-rss && git status --porcelain       # 应只有 3 个未跟踪文件
```
