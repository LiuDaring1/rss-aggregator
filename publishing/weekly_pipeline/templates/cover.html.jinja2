{# 期刊封面模板 #}
<div class="cover">
  {% if manifest.kicker %}<div class="kicker">{{ manifest.kicker }}</div>{% endif %}
  <h1>{{ manifest.title | default("口语素材周刊") }}</h1>
  <div class="issue">{{ manifest.issue_no_label | default(manifest.date_range) }}</div>
</div>
